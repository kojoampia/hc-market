# ===================================================================
# Spring Boot configuration.
#
# This configuration will be overridden by the Spring profile you use,
# for example application-dev.yml if you use the "dev" profile.
#
# More information on profiles: https://www.jhipster.tech/profiles/
# More information on configuration properties: https://www.jhipster.tech/common-application-properties/
# ===================================================================

# ===================================================================
# Standard Spring Boot properties.
# Full reference is available at:
# https://docs.spring.io/spring-boot/appendix/application-properties/index.html
# ===================================================================

---
# Conditionally disable springdoc on missing api-docs profile
spring:
  config:
    activate:
      on-profile: '!api-docs'
springdoc:
  api-docs:
    enabled: false
---
feign:
  circuitbreaker:
    enabled: true
  # client:
  #   config:
  #     default:
  #       connectTimeout: 5000
  #       readTimeout: 5000
management:
  endpoints:
    web:
      base-path: /management
      exposure:
        include:
          - configprops
          - env
          - health
          - info
          - jhimetrics
          - jhiopenapigroups
          - logfile
          - loggers
          - prometheus
          - threaddump
          - caches
          - liquibase
  endpoint:
    health:
      show-details: when_authorized
      roles: 'ROLE_ADMIN'
      probes:
        enabled: true
      group:
        liveness:
          include: livenessState
        readiness:
          include: readinessState,db
    jhimetrics:
      access: read-only
  info:
    git:
      mode: full
    env:
      enabled: true
  health:
    mail:
      enabled: false # When using the MailService, configure an SMTP server and set this to true
  prometheus:
    metrics:
      export:
        enabled: true
        step: 60
  observations:
    key-values:
      application: ${spring.application.name}
  metrics:
    enable:
      http: true
      jvm: true
      logback: true
      process: true
      system: true
    distribution:
      percentiles-histogram:
        all: true
      percentiles:
        all: 0, 0.5, 0.75, 0.95, 0.99, 1.0
    data:
      repository:
        autotime:
          enabled: true
    tags:
      application: ${spring.application.name}

spring:
  application:
    name: healthconnectBooking
  cloud:
    consul:
      discovery:
        healthCheckPath: /management/health
        instanceId: healthconnectbooking:${spring.application.instance-id:${random.value}}
        service-name: healthconnectbooking
      config:
        watch:
          enabled: false
  docker:
    compose:
      enabled: true
      lifecycle-management: start-only
      file: src/main/docker/postgresql.yml
  profiles:
    # The commented value for `active` can be replaced with valid Spring profiles to load.
    # Otherwise, it will be filled in by maven when building the JAR file
    # Either way, it can be overridden by `--spring.profiles.active` value passed in the commandline or `-Dspring.profiles.active` set in `JAVA_OPTS`
    active: '@spring.profiles.active@'
    group:
      dev:
        - secret-samples
        - kafka
        - api-docs
        # Uncomment to activate TLS for the dev profile
        #- tls
      prod:
        - kafka
  jmx:
    enabled: false
  data:
    jpa:
      repositories:
        bootstrap-mode: deferred
  jpa:
    open-in-view: false
    properties:
      hibernate.jdbc.time_zone: UTC
      hibernate.timezone.default_storage: NORMALIZE
      hibernate.type.preferred_instant_jdbc_type: TIMESTAMP
      hibernate.id.new_generator_mappings: true
      hibernate.connection.provider_disables_autocommit: true
      hibernate.cache.use_second_level_cache: true
      hibernate.cache.use_query_cache: false
      hibernate.generate_statistics: false
      # modify batch size as necessary
      hibernate.jdbc.batch_size: 25
      hibernate.order_inserts: true
      hibernate.order_updates: true
      hibernate.query.fail_on_pagination_over_collection_fetch: true
      hibernate.query.in_clause_parameter_padding: true
      hibernate.cache.region.factory_class: com.hazelcast.hibernate.HazelcastCacheRegionFactory
      hibernate.cache.use_minimal_puts: true
      hibernate.cache.hazelcast.instance_name: healthconnectBooking
      hibernate.cache.hazelcast.use_lite_member: true
    hibernate:
      ddl-auto: none
      naming:
        physical-strategy: org.hibernate.boot.model.naming.CamelCaseToUnderscoresNamingStrategy
        implicit-strategy: org.springframework.boot.hibernate.SpringImplicitNamingStrategy
  messages:
    basename: i18n/messages
  main:
    allow-bean-definition-overriding: true
  mvc:
    problemdetails:
      enabled: true
  security:
    oauth2:
      resourceserver:
        jwt:
          authority-prefix: ''
          authorities-claim-name: auth
  task:
    execution:
      thread-name-prefix: healthconnect-booking-task-
      pool:
        core-size: 2
        max-size: 50
        queue-capacity: 10000
    scheduling:
      thread-name-prefix: healthconnect-booking-scheduling-
      pool:
        size: 2
  thymeleaf:
    mode: HTML
  output:
    ansi:
      console-available: true

server:
  servlet:
    session:
      cookie:
        http-only: true

springdoc:
  show-actuator: true

# Properties to be exposed on the /info management endpoint
info:
  # Comma separated list of profiles that will trigger the ribbon to show
  display-ribbon-on-profiles: 'dev'

# ===================================================================
# JHipster specific properties
#
# Full reference is available at: https://www.jhipster.tech/common-application-properties/
# ===================================================================

jhipster:
  mail:
    from: healthconnectBooking@localhost
  api-docs:
    default-include-pattern: /api/**
    management-include-pattern: /management/**
    title: Healthconnect Booking API
    description: Healthconnect Booking API documentation
    version: 0.0.1
    terms-of-service-url:
    contact-name:
    contact-url:
    contact-email:
    license: unlicensed
    license-url:
  security:


# jhipster-needle-add-application-yaml-document
---
# ===================================================================
# Application specific properties
# Add your own application properties here, see the ApplicationProperties class
# to have type-safe configuration, like in the JHipsterProperties above
#
# More documentation is available at:
# https://www.jhipster.tech/common-application-properties/
# ===================================================================

# application:

# ===================================================================
# HealthConnect Marketplace
# NOTE: `jhipster jdl --force` rewrites this file and drops this block.
# ===================================================================
healthconnect:
  seed:
    enabled: false
    file: demo/seed-data.json
    anchor-dates: false
  booking:
    free-cancellation-hours: 24
  # The receipt modal asks payout for the brokerage split. Fails closed: a receipt is a financial
  # statement, and no receipt beats a plausible wrong one.
  payout:
    base-url: http://healthconnectpayout
    timeout-ms: 3000
  # The erasure fan-out -- decisions.md D38. `POST /api/desk/customers/{login}/erase-everywhere`
  # redacts here and then calls messaging's and catalog's desk endpoints with a token minted from the
  # estate signing key. `healthconnect.catalog.base-url` above serves both this and the pricing call;
  # messaging is addressed only from here, and the hostname below resolves in no environment on
  # purpose -- all three compose files set HEALTHCONNECT_MESSAGING_BASE_URL, and CI checks they do.
  #
  # A longer timeout than the pricing calls, and deliberately: those guard a customer waiting on a
  # booking screen, this guards a redaction sweep across several tables. Giving up early would report
  # a leg as failed while it went on succeeding, which is the one wrong answer this receipt must not
  # give.
  messaging:
    base-url: http://healthconnectmessaging
  erasure:
    timeout-ms: 10000
  # Retention -- decisions.md D24, answered by D42. These used to be deliberately absent, on the
  # argument that a default would be a legal position taken by whoever typed it. That argument has
  # been DISCHARGED, not abandoned: counsel ratified these three periods on 2026-09-03, so the values
  # below are attributable rather than invented. They are read from the environment so a deployment
  # can be corrected without a release, with counsel's figures as the fallback.
  #
  # THREE CATEGORIES, because one clock cannot be right for all of them. A period short enough for a
  # message body is far too short for a ledger row the platform must keep; one long enough for the
  # ledger holds health data for six years. The split makes financial rows survive an operational
  # sweep by construction rather than by a condition somebody has to remember to write.
  #
  # care-summary-days is LOAD-BEARING. D42 records counsel's position that the care summary is
  # ordinary contract data rather than special-category, and that rests partly on it being held
  # briefly. Shortening it is safe; lengthening it is a question for counsel, not for whoever is
  # editing the environment file.
  #
  # NOTHING SWEEPS ON ANY OF THIS. There is no scheduler in this estate, and three populated
  # categories look far more like a working retention regime than one unset integer did -- which is
  # why GET /api/desk/privacy reports `enforced: false` beside them.
  #
  # controller-registration has NO fallback, unlike the periods. It is a real identifier belonging to
  # a real organisation: a wrong one is a false claim about a regulatory relationship and a
  # plausible-looking placeholder is worse than an absent value, because it stops anyone asking.
  # Blank counts as absent. This repository is public, which is the second reason it lives in the
  # gitignored environment file rather than here.
  #
  # `pepper` is absent for a third reason -- decisions.md D35. It keys the HMAC that turns an
  # erased customer's login into the alias their rows carry, and this repository is public, so a
  # committed pepper is not a pepper. It is injected exactly as the JWT signing key is: all three
  # compose files require HEALTHCONNECT_PRIVACY_PEPPER, and the value must be the SAME in booking,
  # catalog and messaging or one person acquires three aliases.
  #
  # Absent, this service still starts and everything except the erasure desk works -- that endpoint
  # answers 503. See SubjectPseudonym for why refusing to start would be the worse trade.
  privacy:
    retention:
      financial-days: ${HC_RETENTION_FINANCIAL_DAYS:2190}
      operational-days: ${HC_RETENTION_OPERATIONAL_DAYS:365}
      care-summary-days: ${HC_RETENTION_CARE_SUMMARY_DAYS:90}
    controller-registration: ${HC_DPC_REGISTRATION:}
  #   pepper: injected, never written here

# ===================================================================
# Liquibase must finish BEFORE the seed loader runs.
#
# JHipster defaults application.liquibase.async-start to true, which starts Liquibase on a
# background executor and lets the application finish booting without it. An ApplicationRunner then
# races the schema: SeedDataLoader queries a table Liquibase has not created yet and the seed is
# skipped with "relation does not exist" -- the app comes up healthy and empty, which reads as a
# broken seed file rather than a race.
# ===================================================================
application:
  liquibase:
    async-start: false
