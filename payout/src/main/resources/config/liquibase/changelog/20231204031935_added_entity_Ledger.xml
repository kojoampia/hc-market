<?xml version="1.0" encoding="utf-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:ext="http://www.liquibase.org/xml/ns/dbchangelog-ext"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd
                        http://www.liquibase.org/xml/ns/dbchangelog-ext http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-ext.xsd">

    <!--
        Added the entity Ledger.
    -->
    <changeSet id="20231204031935-1" author="jhipster">
        <createTable tableName="ledger" remarks="One row per completed or late-cancelled booking. `bookingReference` is unique, so a replayed\nbooking.completed event cannot double-credit a professional.\n\nAMENDED, for the same reason `Review` gained `customerLogin` (decisions.md D8): the spec requires\n`/api/pro/**` to resolve the professional from the token and refuse anything that is not the\ncaller&apos;s, but the specified entity carried only `professionalRef` — nothing a JWT subject can be\nmatched against without asking the catalog service. `professionalLogin` makes the ownership check\nlocal to one row.\n\n`deliveryMode` is denormalised here on purpose, exactly as `grossMinor` already is. The earnings\nscreen breaks sessions down by format, and a ledger row must keep saying what it said when it was\nwritten even if the booking is later corrected — the same rule that stops a receipt changing when\na price is edited.\n\nThe money columns are all stored rather than derived, and that is not a contradiction of the\nderived-not-stored rule: commission depends on the BrokerageConfig *in force when the booking\ncompleted*, so recomputing it later from today&apos;s rate would rewrite history. What must never be\nstored is a total ACROSS rows — there is no professional.total_earnings anywhere.">
            <column name="id" type="bigint">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="booking_reference" type="varchar(255)">
                <constraints nullable="false" unique="true" uniqueConstraintName="ux_ledger__booking_reference" />
            </column>
            <column name="professional_ref" type="varchar(255)">
                <constraints nullable="false" />
            </column>
            <column name="professional_login" type="varchar(255)">
                <constraints nullable="false" />
            </column>
            <column name="gross_minor" type="bigint">
                <constraints nullable="false" />
            </column>
            <column name="commission_minor" type="bigint">
                <constraints nullable="false" />
            </column>
            <column name="net_minor" type="bigint">
                <constraints nullable="false" />
            </column>
            <column name="currency" type="varchar(3)">
                <constraints nullable="false" />
            </column>
            <column name="delivery_mode" type="varchar(255)">
                <constraints nullable="false" />
            </column>
            <column name="service_ref" type="varchar(255)">
                <constraints nullable="true" />
            </column>
            <column name="service_name" type="varchar(255)">
                <constraints nullable="true" />
            </column>
            <column name="earned_on" type="date">
                <constraints nullable="false" />
            </column>
            <column name="reversal_of" type="varchar(255)">
                <constraints nullable="true" />
            </column>
            <column name="payout_id" type="bigint">
                <constraints nullable="true" />
            </column>
            <!-- jhipster-needle-liquibase-add-column - JHipster will add columns here -->
        </createTable>
    </changeSet>

    <!-- jhipster-needle-liquibase-add-changeset - JHipster will add changesets here -->

    <!--
        Load sample data generated with Faker.js
        - This data can be easily edited using a CSV editor (or even MS Excel) and
          is located in the 'src/main/resources/config/liquibase/fake-data' directory
        - By default this data is applied when running with the JHipster 'dev' profile.
          This can be customized by adding or removing 'faker' in the 'spring.liquibase.contexts'
          Spring Boot configuration key.
    -->
    <changeSet id="20231204031935-1-data" author="jhipster" context="faker">
        <loadData
                  file="../fake-data/ledger.csv"
                  relativeToChangelogFile="true"
                  separator=";"
                  tableName="ledger"
                  usePreparedStatements="true">
            <column name="id" type="numeric"/>
            <column name="booking_reference" type="string"/>
            <column name="professional_ref" type="string"/>
            <column name="professional_login" type="string"/>
            <column name="gross_minor" type="numeric"/>
            <column name="commission_minor" type="numeric"/>
            <column name="net_minor" type="numeric"/>
            <column name="currency" type="string"/>
            <column name="delivery_mode" type="string"/>
            <column name="service_ref" type="string"/>
            <column name="service_name" type="string"/>
            <column name="earned_on" type="date"/>
            <column name="reversal_of" type="string"/>
            <!-- jhipster-needle-liquibase-add-loadcolumn - JHipster (and/or extensions) can add load columns here -->
        </loadData>
    </changeSet>
</databaseChangeLog>
