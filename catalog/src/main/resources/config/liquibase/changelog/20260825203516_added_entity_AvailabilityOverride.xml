<?xml version="1.0" encoding="utf-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:ext="http://www.liquibase.org/xml/ns/dbchangelog-ext"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd
                        http://www.liquibase.org/xml/ns/dbchangelog-ext http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-ext.xsd">

    <!--
        Added the entity AvailabilityOverride.
    -->
    <changeSet id="20260825203516-1" author="jhipster">
        <createTable tableName="availability_override" remarks="A single day that departs from the rules — a holiday, or a day with different hours.\n\nNamed `AvailabilityOverride`, NOT `AvailabilityException`: a JPA entity whose name ends in\n`Exception` reads as a `Throwable` everywhere it appears, and this repository has already paid\nthat tax once by renaming `Thread` to `Conversation` in messaging. `closed = true` means no slots\nthat day; `closed = false` with a window means those hours instead of the rules&apos;.">
            <column name="id" type="bigint">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="override_date" type="date">
                <constraints nullable="false" />
            </column>
            <column name="closed" type="boolean">
                <constraints nullable="false" />
            </column>
            <column name="start_time" type="${timeType}">
                <constraints nullable="true" />
            </column>
            <column name="end_time" type="${timeType}">
                <constraints nullable="true" />
            </column>
            <column name="note" type="varchar(200)">
                <constraints nullable="true" />
            </column>
            <column name="professional_id" type="bigint">
                <constraints nullable="false" />
            </column>
            <!-- jhipster-needle-liquibase-add-column - JHipster will add columns here -->
        </createTable>
    </changeSet>

    <!-- jhipster-needle-liquibase-add-changeset - JHipster will add changesets here -->

    <!--
        Load sample data generated with Faker.js
        - This data can be easily edited using a CSV editor (or even MS Excel) and
          is located in the 'src/main/resources/config/liquibase/fake-data' directory
        - By default this data is applied when running with the JHipster 'dev' profile.
          This can be customized by adding or removing 'faker' in the 'spring.liquibase.contexts'
          Spring Boot configuration key.
    -->
    <changeSet id="20260825203516-1-data" author="jhipster" context="faker">
        <loadData
                  file="../fake-data/availability_override.csv"
                  relativeToChangelogFile="true"
                  separator=";"
                  tableName="availability_override"
                  usePreparedStatements="true">
            <column name="id" type="numeric"/>
            <column name="override_date" type="date"/>
            <column name="closed" type="boolean"/>
            <column name="start_time" type="time"/>
            <column name="end_time" type="time"/>
            <column name="note" type="string"/>
            <column name="professional_id" type="numeric"/>
            <!-- jhipster-needle-liquibase-add-loadcolumn - JHipster (and/or extensions) can add load columns here -->
        </loadData>
    </changeSet>
</databaseChangeLog>
