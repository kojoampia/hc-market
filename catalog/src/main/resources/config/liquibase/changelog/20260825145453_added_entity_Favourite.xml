<?xml version="1.0" encoding="utf-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:ext="http://www.liquibase.org/xml/ns/dbchangelog-ext"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd
                        http://www.liquibase.org/xml/ns/dbchangelog-ext http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-ext.xsd">

    <!--
        Added the entity Favourite.
    -->
    <changeSet id="20260825145453-1" author="jhipster">
        <createTable tableName="favourite" remarks="The customer&apos;s Saved list.\n\nDeliberately NOT a relationship to Professional. A favourite is keyed by the professional&apos;s\nbusiness reference and the customer&apos;s login, both of which are stable and meaningful outside this\nservice; a foreign key would mean a customer&apos;s saved list could not survive a professional being\nremoved from the catalogue, and &quot;the person I saved is gone&quot; is a thing the screen should be able\nto say rather than a row that vanishes.\n\n`customerLogin` + `professionalRef` is unique: saving twice is the same as saving once, and the\nschema is what makes that true rather than a check in the resource.">
            <column name="id" type="bigint">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="customer_login" type="varchar(255)">
                <constraints nullable="false" />
            </column>
            <column name="professional_ref" type="varchar(255)">
                <constraints nullable="false" />
            </column>
            <column name="added_at" type="${datetimeType}">
                <constraints nullable="false" />
            </column>
            <!-- jhipster-needle-liquibase-add-column - JHipster will add columns here -->
        </createTable>
        <dropDefaultValue tableName="favourite" columnName="added_at" columnDataType="${datetimeType}"/>
    </changeSet>

    <!-- jhipster-needle-liquibase-add-changeset - JHipster will add changesets here -->

    <!--
        Load sample data generated with Faker.js
        - This data can be easily edited using a CSV editor (or even MS Excel) and
          is located in the 'src/main/resources/config/liquibase/fake-data' directory
        - By default this data is applied when running with the JHipster 'dev' profile.
          This can be customized by adding or removing 'faker' in the 'spring.liquibase.contexts'
          Spring Boot configuration key.
    -->
    <changeSet id="20260825145453-1-data" author="jhipster" context="faker">
        <loadData
                  file="../fake-data/favourite.csv"
                  relativeToChangelogFile="true"
                  separator=";"
                  tableName="favourite"
                  usePreparedStatements="true">
            <column name="id" type="numeric"/>
            <column name="customer_login" type="string"/>
            <column name="professional_ref" type="string"/>
            <column name="added_at" type="date"/>
            <!-- jhipster-needle-liquibase-add-loadcolumn - JHipster (and/or extensions) can add load columns here -->
        </loadData>
    </changeSet>
</databaseChangeLog>
