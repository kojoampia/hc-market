node_modules
package-lock.json
.git

# Generated by jhipster:client
target/classes/static/

# Build output, and a second checkout: `.claude/` would hold an agent git worktree if one were ever
# rooted here, and a formatter that walks from the root would rewrite somebody else's files. The
# same reasoning as `eslint.config.ts`'s ignore — see the note there for what was measured.
target/
.claude/
